import type { User, Product, Order, Design } from "@/types"

// This file is no longer needed as data is now fetched from MongoDB.
// Keeping it as a placeholder for reference if needed.
export const mockUsers: User[] = []
export const mockProducts: Product[] = []
export const mockOrders: Order[] = []
export const mockDesigns: Design[] = []
